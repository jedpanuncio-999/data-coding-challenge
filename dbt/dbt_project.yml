name: venatus_data
version: 1.0.0
config-version: 2
profile: venatus_data

model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots
target-path: target
clean-targets:
  - target
  - dbt_packages

models:
  venatus_data:
    example:
      +materialized: view
    staging:
      +materialized: view
    marts:
      +materialized: table
